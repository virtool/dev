[tool.poetry]
name = "dev"
version = "0.1.0"
description = ""
authors = ["Reece Hoffmann <reecehoffmann@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.2"
pyyaml = "^6.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
