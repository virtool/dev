images:
  - name: ghcr.io/virtool/ui
    newTag: 7.4.3

labels:
  - pairs:
      app: virtool-ui
    includeSelectors: true
    includeTemplates: true

namePrefix: virtool-

resources:
  - deployment.yaml
  - service.yaml